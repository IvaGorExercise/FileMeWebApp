@if (Request.IsAuthenticated)
{
     <div style="margin-top:7%;" class="btn-group">
        <a class="btn btn-default dropdown-toggle" href="#" data-toggle="dropdown"><i class="glyphicon glyphicon-user"></i><strong> @User.Identity.Name </strong><span class="caret"></span></a>
        <ul class="dropdown-menu pull-right">
        
            <li>@Html.ActionLink("Pitanja", "Pitanja", "Manage", routeValues: null, htmlAttributes: new { @class = "username", title = "Pitanja" })</li>
            <li>@Html.ActionLink("Izmjena lozinke", "Manage", "Account", routeValues: null, htmlAttributes: new { @class = "username", title = "Manage" })</li>
         <li><a href="javascript:document.getElementById('logoutForm').submit()">Odjava</a>
                @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
                {
                    @Html.AntiForgeryToken()
           
                }
            </li>

        </ul>
    </div>
}

else


{

       <ul class="nav navbar-nav">

     @*   <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>*@
        <li>@Html.ActionLink("LOG IN", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>

    @*    <li class="@(ViewContext.RouteData.GetRequiredString("action") == "Login" ? "active" : "")">@Html.ActionLink("Prijava", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>*@
       
       </ul>
}
